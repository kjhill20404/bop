

class math:
    """
    docstring
    """
    def __init__(self,Num1,Num2):
        self.Num1 = Num1 
        self.Num2 = Num2
    def add(Num1,Num2):
        if type(Num1) == int or float and type(Num2) == int or float:
            sum = int(Num1) + int(Num2)
            print(sum)
        else:
            print('please enter a number').upper
N = input('what the heck in the first number???')
N2 = input('what the heck in the second number???')
math(N,N2)
math.add(N,N2)